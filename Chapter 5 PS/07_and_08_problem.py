d = {}
name = input ("Enter fiends name: ")
lang = input("Enter your favourite language: ")
d.update({name : lang})

name = input ("Enter fiends name: ")
lang = input("Enter your favourite language: ")
d.update({name : lang})

name = input ("Enter fiends name: ")
lang = input("Enter your favourite language: ")
d.update({name : lang})

name = input ("Enter fiends name: ")
lang = input("Enter your favourite language: ")
d.update({name : lang})

print(d)

#Q7 if 2 keys(here name) are same (& as we are using update operation) then 2nd value(here language) of key will we updated after the running of 2nd key code
#Q8 if we have same values for some key then it will get associated to both